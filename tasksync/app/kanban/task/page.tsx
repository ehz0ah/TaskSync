import { Board } from "../../(platform)/_parts/task";

export default function Kanban () {
    return (
        <div className="h-full w-full bg-neutral-900 text-neutral-50">
            <Board />
        </div>
    );
}