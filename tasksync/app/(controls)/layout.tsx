const SettingsPage = ({ children }: { children: React.ReactNode }) => {
    return (
      <div >
        <main>
            {children}
        </main>
      </div>
    );
  };
  
  export default SettingsPage;

  // className="bg-gradient-to-r from-purple-400 via-teal-400 to-pink-400 h-full flex items-center w-full justify-center"